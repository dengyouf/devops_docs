apiVersion: v1
kind: Service
metadata:
  labels:
    component: elastic
  name: elastic
spec:
  type: ClusterIP
  selector:
    component: elastic
  ports:
  - name: http
    port: 9200
    protocol: TCP
    targetPort: 9200
